import React from 'react';
import { createComponent } from '@lit/react';
import { DsLabel } from '@dx/core';

export const Label = createComponent({
  tagName: 'ds-label',
  elementClass: DsLabel,
  react: React,
  events: {},
});
