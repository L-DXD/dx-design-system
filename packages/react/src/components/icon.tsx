import React from 'react';
import { createComponent } from '@lit/react';
import { DsIcon } from '@dx/core';

export const Icon = createComponent({
  tagName: 'ds-icon',
  elementClass: DsIcon,
  react: React,
  events: {},
});
