import React from 'react';
import { createComponent } from '@lit/react';
import { DsOption } from '@dx/core';

export const Option = createComponent({
  tagName: 'ds-option',
  elementClass: DsOption,
  react: React,
  events: {},
});
